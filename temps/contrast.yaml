type: markdown
css: |
  @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&family=IBM+Plex+Mono:wght@400;600&display=swap');

  .md-contrast {
    --md-accent: #0ea5e9;
    --md-accent-strong: #0284c7;
    font-family: "Space Grotesk", "Segoe UI", sans-serif;
    font-size: 16px;
    line-height: 1.7;
    color: var(--shell-text, #0f172a);
    max-width: 900px;
    margin: 0 auto;
  }
  .md-contrast h1,
  .md-contrast h2,
  .md-contrast h3 {
    font-weight: 700;
    letter-spacing: 0.02em;
    line-height: 1.15;
    margin: 1.6em 0 0.6em;
  }
  .md-contrast h1 {
    font-size: 2.4rem;
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }
  .md-contrast h2 {
    font-size: 1.7rem;
    border-left: 4px solid var(--md-accent);
    padding-left: 0.6rem;
  }
  .md-contrast h3 { font-size: 1.3rem; }
  .md-contrast a {
    color: var(--md-accent);
    text-decoration: none;
    background: color-mix(in srgb, var(--md-accent) 12%, transparent);
    padding: 0 0.2em;
    border-radius: 0.2em;
  }
  .md-contrast a:hover {
    background: color-mix(in srgb, var(--md-accent-strong) 20%, transparent);
  }
  .md-contrast blockquote {
    border-left: 4px solid var(--md-accent-strong);
    padding: 0.9em 1.1em;
    margin: 1.2em 0;
    background: color-mix(in srgb, var(--md-accent) 8%, transparent);
  }
  .md-contrast code {
    font-family: "IBM Plex Mono", "Fira Mono", monospace;
    font-size: 0.95em;
    padding: 0.12em 0.35em;
    border-radius: 0.25rem;
    background: color-mix(in srgb, var(--md-accent) 16%, transparent);
  }
  .md-contrast pre {
    margin: 1.1em 0;
    padding: 1em 1.1em;
    border-radius: 0.7rem;
    border: 1px solid color-mix(in srgb, var(--md-accent) 24%, transparent);
    background: rgba(15, 23, 42, 0.04);
    overflow-x: auto;
  }
  .md-contrast hr {
    border: none;
    border-top: 2px solid color-mix(in srgb, var(--md-accent) 30%, transparent);
    margin: 2em 0;
  }
  .md-contrast .markdown-callout {
    border: 1px solid color-mix(in srgb, var(--md-accent) 45%, transparent);
    border-left: 4px solid var(--md-accent);
    border-radius: 0.6rem;
    padding: 0.9rem 1.1rem;
    margin: 1.1rem 0;
    background: color-mix(in srgb, var(--md-accent) 8%, transparent);
  }
  .md-contrast .markdown-callout-title {
    text-transform: uppercase;
    letter-spacing: 0.08em;
    font-size: 0.82rem;
    margin-bottom: 0.4rem;
    color: var(--md-accent-strong);
  }
  .md-contrast .markdown-qa {
    border: 1px solid color-mix(in srgb, var(--md-accent) 35%, transparent);
    border-radius: 0.75rem;
    padding: 0.95rem 1.1rem;
    margin: 1.1rem 0;
    background: rgba(14, 165, 233, 0.08);
  }
  .md-contrast details.markdown-qa summary {
    cursor: pointer;
    list-style: none;
    display: flex;
    align-items: center;
    gap: 0.6rem;
  }
  .md-contrast details.markdown-qa summary::-webkit-details-marker {
    display: none;
  }
  .md-contrast .markdown-qa-toggle {
    margin-left: auto;
    font-size: 0.75rem;
    letter-spacing: 0.08em;
    text-transform: uppercase;
    color: var(--md-accent);
  }
  .md-contrast .markdown-media {
    border: 1px solid color-mix(in srgb, var(--md-accent) 30%, transparent);
    border-radius: 0.7rem;
    padding: 0.8rem;
    margin: 1.1rem 0;
    background: rgba(15, 23, 42, 0.04);
  }
  .md-contrast .markdown-media audio,
  .md-contrast .markdown-media video {
    width: 100%;
    display: block;
    border-radius: 0.6rem;
  }
  .md-contrast .markdown-img-grid {
    display: grid;
    gap: 14px;
    margin: 1.1rem 0;
  }
  .md-contrast .markdown-img-grid.two-col,
  .md-contrast .markdown-img-grid.two-vertical {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .md-contrast .markdown-img-grid.four-grid {
    grid-template-columns: repeat(2, minmax(0, 1fr));
  }
  .md-contrast .markdown-img-grid.rail {
    grid-auto-flow: column;
    grid-auto-columns: minmax(200px, 1fr);
    overflow-x: auto;
    padding-bottom: 6px;
  }
  .md-contrast .markdown-cover {
    border: 1px solid color-mix(in srgb, var(--md-accent) 30%, transparent);
    border-radius: 1rem;
    padding: 1.4rem 1.2rem;
    background: linear-gradient(135deg, rgba(14, 165, 233, 0.12), rgba(15, 23, 42, 0.02));
  }
  .md-contrast .markdown-banner {
    border-radius: 0.8rem;
    padding: 0.9rem 1.1rem;
    background: color-mix(in srgb, var(--md-accent) 12%, transparent);
  }
  .md-contrast .markdown-notes {
    border-left: 3px solid var(--md-accent);
    padding-left: 0.9rem;
    color: color-mix(in srgb, currentColor 70%, transparent);
  }
  .md-contrast .markdown-kpi,
  .md-contrast .markdown-features {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    margin: 1.2rem 0;
  }
  .md-contrast .markdown-kpi > *,
  .md-contrast .markdown-features > * {
    border: 1px solid color-mix(in srgb, var(--md-accent) 45%, transparent);
    border-radius: 0.8rem;
    padding: 1rem 1.1rem;
    background: rgba(14, 165, 233, 0.08);
  }
  .md-contrast .markdown-cols {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    margin: 1.1rem 0;
  }
  .md-contrast .markdown-code-split {
    display: grid;
    gap: 1rem;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    margin: 1.2rem 0;
  }
  .md-contrast .markdown-code-split pre {
    margin: 0;
  }
  .md-contrast .markdown-highlight {
    background: color-mix(in srgb, var(--md-accent) 24%, transparent);
    border-radius: 0.25rem;
    padding: 0 0.25rem;
  }
  .md-contrast .markdown-blur {
    filter: blur(4px);
    transition: filter 0.2s ease;
  }
  .md-contrast .markdown-blur:hover,
  .md-contrast .markdown-blur:focus {
    filter: blur(0);
  }
wrapperClass: md-contrast
exportCss: |
  body.markdown-export {
    background:
      radial-gradient(circle at 15% 10%, rgba(34, 211, 238, 0.18), transparent 45%),
      radial-gradient(circle at 85% 0%, rgba(14, 165, 233, 0.12), transparent 50%),
      linear-gradient(180deg, #05070b 0%, #0b0f14 55%, #0b111a 100%);
    color: #e5e7eb;
    font-family: "Space Grotesk", "Segoe UI", sans-serif;
    margin: 0;
  }
  body.markdown-export .markdown-export-content {
    max-width: 940px;
    margin: 40px auto;
    padding: 48px 52px;
    background: #0f172a;
    border: 1px solid #1f2937;
    border-radius: 18px;
    box-shadow: 0 26px 70px rgba(0, 0, 0, 0.45);
    animation: md-contrast-rise 0.55s ease-out both;
  }
  body.markdown-export .md-contrast a {
    color: #22d3ee;
  }
  body.markdown-export pre {
    background: #111827;
    border-color: #1f2937;
  }
  .md-now {
    position: fixed;
    right: 18px;
    top: 18px;
    padding: 0.4rem 0.75rem;
    border-radius: 999px;
    border: 1px solid rgba(34, 211, 238, 0.45);
    background: rgba(15, 23, 42, 0.8);
    color: #22d3ee;
    font-size: 0.8rem;
    letter-spacing: 0.02em;
    z-index: 1000;
    max-width: 240px;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }
  @keyframes md-contrast-rise {
    from { opacity: 0; transform: translateY(12px); }
    to { opacity: 1; transform: translateY(0); }
  }
  @media (prefers-reduced-motion: reduce) {
    body.markdown-export .markdown-export-content {
      animation: none;
    }
  }
customHead: |
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;600;700&family=IBM+Plex+Mono:wght@400;600&display=swap" rel="stylesheet">
customBody: |
  <script>
  (() => {
    const root = document.querySelector('.markdown-export-content');
    if (!root) return;
    const headings = Array.from(root.querySelectorAll('h1, h2, h3'));
    if (!headings.length) return;

    const pill = document.createElement('div');
    pill.className = 'md-now';
    document.body.appendChild(pill);

    const update = () => {
      let current = headings[0];
      for (const heading of headings) {
        const rect = heading.getBoundingClientRect();
        if (rect.top <= 140) {
          current = heading;
        } else {
          break;
        }
      }
      const text = (current.textContent || '').trim();
      pill.textContent = text || 'Section';
    };

    update();
    window.addEventListener('scroll', update, {passive: true});
  })();
  </script>
